# setting.py:从setting.json中读取配置数据，并将其转换成字典。
#def load_config(path: str | None = None) -> dict:中->dict表示该函数的返回值是dict

#data_factory.py:测试数据工厂，生成参数及对应值，json数据。（用于post——>Body_raw 'json' )
#自动生成不重复、合法的测试数据（比如每次都不一样的邮箱）
#统一管理所有接口需要的请求体模板，避免用例里到处写重复的字典
#unique_email()：生成唯一邮箱:用 时间戳 + 随机字符,保证每次调用都生成一个不重复的邮箱,解决接口自动化里最常见的问题：“邮箱已被注册” 导致用例失败
#user_payload():生成完整的用户注册请求体：把接口注册时需要的所有字段（姓名、邮箱、地址、手机号等），封装成一个固定模板，自动把 unique_email() 生成的邮箱填进去，用例里直接调用这个函数，就能拿到一个完整、合法、可直接发送的请求体